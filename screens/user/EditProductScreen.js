import React from "react";
import { Text, View, TextInput, ScrollView } from "react-native";

const EditProductScreen = (props) => {
    return (
        <ScrollView>
        <View>
            <Text>Title Edit Product Screen</Text>
        </View>
        </ScrollView>
    )
}

export default EditProductScreen;